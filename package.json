{
  "name": "roxysu",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "trustedDependencies": [
    "better-sqlite3",
    "realm"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd apps/server dev\" \"bun run --filter '@roxysu/realm-reader' dev\" \"tosu\"",
    "test": "bun run --cwd apps/server test && bun run --filter '@roxysu/pattern-7k' test && bun run --filter '@roxysu/timing-analysis' test && bun test packages/osu-paths packages/mania-judge packages/realm-backup",
    "typecheck": "bunx tsc -p apps/server --noEmit && bunx tsc -p apps/realm-reader --noEmit && bunx tsc -p packages/db --noEmit && bunx tsc -p packages/osu-paths --noEmit && bunx tsc -p packages/mania-judge --noEmit && bunx tsc -p packages/sunny-dan --noEmit && bunx tsc -p packages/collection-sync --noEmit && bunx tsc -p packages/realm-backup --noEmit && bunx tsc -p packages/osu-chart --noEmit && bunx tsc -p packages/pattern-7k --noEmit && bunx tsc -p packages/timing-analysis --noEmit"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
