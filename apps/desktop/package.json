{
  "name": "@roxysu/desktop",
  "productName": "Roxysu",
  "version": "0.1.13",
  "private": true,
  "description": "Roxysu Electron shell (Windows product path; NixOS/Linux for smoke-testing)",
  "main": "main.js",
  "scripts": {
    "dev": "bun run --cwd ../server build:ui && node ./scripts/run-electron.mjs .",
    "start": "node ./scripts/run-electron.mjs .",
    "dev:smoke": "node ./scripts/run-electron.mjs .",
    "build:stage": "node ./scripts/build-pack.mjs",
    "rebuild:native": "node ./scripts/rebuild-native.mjs",
    "archive:realm": "node ./scripts/archive-realm.mjs",
    "build:launcher": "node ./scripts/build-win-launcher.mjs",
    "pack": "bun run build:stage && bun run rebuild:native && bun run archive:realm",
    "assert:packaged": "node ./scripts/assert-packaged.mjs",
    "assert:linux-resources": "node ./scripts/assert-linux-resources.mjs",
    "pack:linux-resources": "node ./scripts/pack-linux-resources.mjs",
    "dist:linux-resources": "bun run pack && bun run pack:linux-resources && bun run assert:linux-resources",
    "dist:win": "bun run build:launcher && bun run pack && electron-builder --win --publish never && bun run assert:packaged",
    "dist:dir": "bun run pack && electron-builder --dir --publish never && bun run assert:packaged"
  },
  "build": {
    "appId": "dev.roxysu.desktop",
    "productName": "Roxysu",
    "directories": {
      "output": "release"
    },
    "afterPack": "./scripts/after-pack.mjs",
    "icon": "../server/public/icons/icon-512.png",
    "publish": [
      {
        "provider": "github",
        "owner": "Yon-Luc",
        "repo": "Roxysu"
      }
    ],
    "files": [
      "main.js",
      "auto-update.js",
      "preload.js",
      "paths.js",
      "splash.html"
    ],
    "extraResources": [
      {
        "from": "stage/public",
        "to": "public"
      },
      {
        "from": "stage/server",
        "to": "server"
      },
      {
        "from": "stage/node",
        "to": "node"
      },
      {
        "from": "stage/splash.html",
        "to": "splash.html"
      },
      {
        "from": "stage/realm-reader.tgz",
        "to": "realm-reader.tgz"
      }
    ],
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "../server/public/icons/icon-512.png",
      "artifactName": "${productName}-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "artifactName": "${productName}-Setup-${version}.${ext}"
    }
  },
  "dependencies": {
    "electron-updater": "^6.6.2"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.2",
    "electron": "^42.0.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.25.0"
  }
}
