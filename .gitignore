# dependencies
node_modules/

# build outputs
dist/
apps/desktop/stage/
apps/desktop/.node-preserve/
apps/desktop/release/
apps/desktop/launcher/win/dist/
*.tsbuildinfo

# .NET / mania-rating-calc publish artifacts
**/bin/
**/obj/
*.user
*.suo
roxysu-calc/

# local env overrides (see .env.example)
.env
.env.local

# sqlite runtime data — don't commit the actual local db
*.sqlite
*.sqlite-shm
*.sqlite-wal

# realm db backups — created before collection sync writes to client.realm
backups/

# nix
.direnv/
result
result-*

# OS / editor
.DS_Store
*.swp
*~

# local branch pin / scratch (not source)
.branch-*.json

# apps/overlay build artifact
apps/overlay/roxysu-overlay
# apps/overlay generated wayland protocol code
apps/overlay/gen/
